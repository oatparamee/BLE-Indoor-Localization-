import SwiftUI

// MARK: - App Entry Point
// The @main attribute marks this as the application's entry point.
// SwiftUI manages the app lifecycle automatically.

@main
struct BLEScannerApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
