/**
 * Backend API configuration.
 *
 * CHANGE THIS to match the IP address of the machine running the Flask backend.
 * The phone must be on the same local network as the backend.
 */
export const API_BASE_URL = 'http://192.168.1.100:5000';
